# Ritim

Ritim is an Android Compose prototype focused on a liquid-glass bottom navigation bar.

## Build

```sh
./gradlew :app:assembleDebug
```

The GitHub Actions workflow uploads the debug APK artifact on pushes to `main`, pull requests, and manual runs.
